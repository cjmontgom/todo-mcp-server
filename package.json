{
    "name": "todo-mcp-server",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "start": "node build/index.js",
        "dev": "concurrently --names \"server,proxy,client\" --prefix-colors \"blue,yellow,green\" \"npm run start\" \"npm run dev --prefix proxy\" \"npm run dev --prefix client\""
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.1.0"
    },
    "devDependencies": {
        "@types/node": "^22.10.5",
        "concurrently": "^9.2.1",
        "typescript": "^5.7.2"
    }
}
